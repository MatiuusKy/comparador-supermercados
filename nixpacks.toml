[phases.setup]
nixPkgs = ["python3", "python3Packages.pip", "chromium", "nss", "nspr", "atk", "cups", "dbus", "libdrm", "gtk3", "pango", "cairo", "xorg.libX11", "xorg.libXcomposite", "xorg.libXdamage", "xorg.libXext", "xorg.libXfixes", "xorg.libXrandr", "xorg.libxcb", "mesa", "expat", "libxkbcommon", "alsa-lib"]

[phases.install]
cmds = ["python3 -m pip install -r requirements.txt", "python3 -m playwright install chromium"]

[start]
cmd = "uvicorn app:app --host 0.0.0.0 --port $PORT"
